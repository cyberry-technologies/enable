apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: enable-service-api-execution-ingress
  namespace: enable
spec:
  rules:
  - host: api.localhost
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: enable-service-api-execution-service
            port:
              number: 8081