apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: enable-database-execution-pvc
    namespace: enable
spec:
    accessModes:
        - ReadWriteOnce
    resources:
        requests:
            storage: 10Gi
    storageClassName: local-path 